export var prefixSelectors = function prefixSelectors(tag, selectors, style) {
  return selectors.map(function (selector) {
    return "".concat(tag, "::-").concat(selector, " ").concat(style);
  });
};