import { Base16Theme } from 'base16';
import * as CSS from 'csstype';
export declare type BorderPosition = 'top' | 'bottom';
export interface Props {
    fullHeight?: boolean;
    compact?: boolean;
    borderPosition?: BorderPosition;
    noBorder?: boolean;
}
declare const Toolbar: import("styled-components").StyledComponent<"div", Base16Theme & {
    fontFamily?: CSS.Property.FontFamily | undefined;
}, Props, never>;
export default Toolbar;
