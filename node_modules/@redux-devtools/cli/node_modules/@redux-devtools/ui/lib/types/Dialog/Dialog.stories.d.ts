import { Story } from '@storybook/react';
import Dialog from './';
import { DialogProps } from './Dialog';
import { Props as FormProps } from '../Form/Form';
declare const _default: {
    title: string;
    component: typeof Dialog;
};
export default _default;
export declare const Default: Story<DialogProps | (DialogProps & FormProps<unknown>)>;
export declare const WithForm: Story<DialogProps | (DialogProps & FormProps<unknown>)>;
