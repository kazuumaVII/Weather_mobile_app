import { Position } from '../Tabs';
interface StyleProps {
    position: Position;
}
export declare const TabsContainer: import("styled-components").StyledComponent<"div", any, StyleProps, never>;
export {};
