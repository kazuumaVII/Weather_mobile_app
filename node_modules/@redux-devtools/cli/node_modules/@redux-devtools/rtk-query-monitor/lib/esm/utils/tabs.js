import _typeof from "@babel/runtime/helpers/typeof";
export function isTabVisible(tab, visKey) {
  if (typeof tab.visible === 'boolean') {
    return tab.visible;
  }

  if (_typeof(tab.visible) === 'object' && tab.visible) {
    return !!tab.visible[visKey];
  }

  return true;
}