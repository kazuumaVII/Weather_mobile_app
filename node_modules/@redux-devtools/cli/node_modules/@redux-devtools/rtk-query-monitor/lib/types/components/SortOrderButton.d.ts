export interface SortOrderButtonProps {
    readonly isAsc?: boolean;
    readonly onChange: (isAsc: boolean) => void;
    id?: string;
}
export declare function SortOrderButton({ isAsc, onChange, id, }: SortOrderButtonProps): JSX.Element;
