import { RtkResourceInfo, SelectOption } from '../types';
export interface FilterList<T> {
    (regex: RegExp | null, list: T[]): T[];
}
export declare enum QueryFilters {
    queryKey = "query key",
    reducerPath = "reducerPath",
    endpointName = "endpoint",
    status = "status"
}
export declare const filterQueryOptions: SelectOption<QueryFilters>[];
export declare const queryListFilters: Readonly<Record<QueryFilters, FilterList<RtkResourceInfo>>>;
