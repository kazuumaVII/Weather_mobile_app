import React from 'react';
import PropTypes from 'prop-types';
import { JSONTree } from 'react-json-tree';
import _getItemString from './getItemString';
import getJsonTreeTheme from './getJsonTreeTheme';

var StateTab = function StateTab(_ref) {
  var nextState = _ref.nextState,
      styling = _ref.styling,
      base16Theme = _ref.base16Theme,
      invertTheme = _ref.invertTheme,
      labelRenderer = _ref.labelRenderer,
      dataTypeKey = _ref.dataTypeKey,
      isWideLayout = _ref.isWideLayout;
  return /*#__PURE__*/React.createElement(JSONTree, {
    labelRenderer: labelRenderer,
    theme: getJsonTreeTheme(base16Theme),
    data: nextState,
    getItemString: function getItemString(type, data) {
      return _getItemString(styling, type, data, dataTypeKey, isWideLayout);
    },
    invertTheme: invertTheme,
    hideRoot: true
  });
};

StateTab.propTypes = {
  nextState: PropTypes.any.isRequired,
  styling: PropTypes.func.isRequired,
  base16Theme: PropTypes.any.isRequired,
  invertTheme: PropTypes.bool.isRequired,
  labelRenderer: PropTypes.func.isRequired,
  dataTypeKey: PropTypes.oneOfType([PropTypes.string, PropTypes.symbol]),
  isWideLayout: PropTypes.bool.isRequired
};
export default StateTab;