import { Dispatch, MiddlewareAPI } from 'redux';
import { StoreAction } from '../actions';
import { StoreState } from '../reducers';
export declare const exportStateMiddleware: (store: MiddlewareAPI<Dispatch<StoreAction>, StoreState>) => (next: Dispatch<StoreAction>) => (action: StoreAction) => StoreAction;
