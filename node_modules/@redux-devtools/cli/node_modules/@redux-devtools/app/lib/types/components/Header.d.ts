import React, { Component } from 'react';
import { ResolveThunks } from 'react-redux';
import { changeSection } from '../actions';
declare type DispatchProps = ResolveThunks<typeof actionCreators>;
interface OwnProps {
    readonly section: string;
}
declare type Props = DispatchProps & OwnProps;
declare class Header extends Component<Props> {
    openLink: (url: string) => () => void;
    render(): JSX.Element;
}
declare const actionCreators: {
    changeSection: typeof changeSection;
};
declare const _default: import("react-redux").ConnectedComponent<typeof Header, import("react-redux").Omit<React.ClassAttributes<Header> & {
    changeSection: typeof changeSection;
} & OwnProps, "changeSection">>;
export default _default;
