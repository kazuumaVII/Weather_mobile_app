import React, { Component } from 'react';
import { ResolveThunks } from 'react-redux';
import { toggleDispatcher } from '../../actions';
declare type DispatchProps = ResolveThunks<typeof actionCreators>;
interface OwnProps {
    dispatcherIsOpen: boolean;
}
declare type Props = DispatchProps & OwnProps;
declare class DispatcherButton extends Component<Props> {
    shouldComponentUpdate(nextProps: Props): boolean;
    render(): JSX.Element;
}
declare const actionCreators: {
    toggleDispatcher: typeof toggleDispatcher;
};
declare const _default: import("react-redux").ConnectedComponent<typeof DispatcherButton, import("react-redux").Omit<React.ClassAttributes<DispatcherButton> & {
    toggleDispatcher: typeof toggleDispatcher;
} & OwnProps, "toggleDispatcher">>;
export default _default;
