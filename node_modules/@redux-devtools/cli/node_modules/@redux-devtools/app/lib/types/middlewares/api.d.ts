import { Dispatch, MiddlewareAPI } from 'redux';
import { StoreAction } from '../actions';
import { StoreState } from '../reducers';
export declare function api(inStore: MiddlewareAPI<Dispatch<StoreAction>, StoreState>): (next: Dispatch<StoreAction>) => (action: StoreAction) => StoreAction;
