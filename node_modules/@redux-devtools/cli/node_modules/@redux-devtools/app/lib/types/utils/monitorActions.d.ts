import { InstancesState, State } from '../reducers/instances';
import { Dispatch, MiddlewareAPI } from 'redux';
import { DispatchAction, StoreActionWithoutLiftedAction } from '../actions';
export declare function sweep(state: State): State;
export declare function nonReduxDispatch(store: MiddlewareAPI<Dispatch<StoreActionWithoutLiftedAction>, {
    readonly instances: InstancesState;
}>, message: string, instanceId: string | number, action: DispatchAction, initialState: string | undefined, preInstances?: InstancesState): string | undefined;
