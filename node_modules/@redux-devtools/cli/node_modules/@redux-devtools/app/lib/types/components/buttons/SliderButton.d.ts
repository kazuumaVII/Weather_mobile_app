import React, { Component } from 'react';
import { ResolveThunks } from 'react-redux';
import { toggleSlider } from '../../actions';
declare type DispatchProps = ResolveThunks<typeof actionCreators>;
interface OwnProps {
    isOpen: boolean;
}
declare type Props = DispatchProps & OwnProps;
declare class SliderButton extends Component<Props> {
    shouldComponentUpdate(nextProps: Props): boolean;
    render(): JSX.Element;
}
declare const actionCreators: {
    toggleSlider: typeof toggleSlider;
};
declare const _default: import("react-redux").ConnectedComponent<typeof SliderButton, import("react-redux").Omit<React.ClassAttributes<SliderButton> & {
    toggleSlider: typeof toggleSlider;
} & OwnProps, "toggleSlider">>;
export default _default;
