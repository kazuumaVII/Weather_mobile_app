import { StoreAction } from '../actions';
interface Notification {
    readonly type: 'error';
    readonly message: string;
}
export declare type NotificationState = Notification | null;
export declare function notification(state: NotificationState | undefined, action: StoreAction): NotificationState;
export {};
