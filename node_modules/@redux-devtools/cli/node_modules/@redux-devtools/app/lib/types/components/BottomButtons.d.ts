import { Component } from 'react';
import PropTypes from 'prop-types';
import { Options } from '../reducers/instances';
interface Props {
    dispatcherIsOpen: boolean;
    sliderIsOpen: boolean;
    options: Options;
}
export default class BottomButtons extends Component<Props> {
    static propTypes: {
        dispatcherIsOpen: PropTypes.Requireable<boolean>;
        sliderIsOpen: PropTypes.Requireable<boolean>;
        options: PropTypes.Validator<object>;
    };
    shouldComponentUpdate(nextProps: Props): boolean;
    render(): JSX.Element;
}
export {};
