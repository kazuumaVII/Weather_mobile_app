export var name = 'Mocha template';
export var dispatcher = '${action};';
export var assertion = 'expect(store${path}).toEqual(${curState});';
export var wrap = "import expect from 'expect';\nimport ${name} from '../../stores/${name}';\n\ntest('${name}', (t) => {\n  const store = new ${name}(${initialState});\n  ${assertions}\n});\n";
export default {
  name: name,
  assertion: assertion,
  dispatcher: dispatcher,
  wrap: wrap
};