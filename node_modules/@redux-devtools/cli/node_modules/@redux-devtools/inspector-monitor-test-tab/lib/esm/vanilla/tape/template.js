export var name = 'Tape template';
export var dispatcher = '${action};';
export var assertion = 't.deepEqual(state${path}, ${curState});';
export var wrap = "import test from 'tape';\nimport ${name} from '../../stores/${name}';\n\ntest('${name}', (t) => {\n  const store = new ${name}(${initialState});\n  ${assertions}\n  t.end();\n});\n";
export default {
  name: name,
  assertion: assertion,
  dispatcher: dispatcher,
  wrap: wrap
};