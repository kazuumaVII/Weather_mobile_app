export var name = 'Mocha template';
export var dispatcher = 'state = reducers(${prevState}, ${action});';
export var assertion = 'expect(state).toEqual(${curState});';
export var wrap = "import expect from 'expect';\nimport reducers from '../../reducers';\n\ndescribe('reducers', () => {\n  it('should handle actions', () => {\n    let state;\n    ${assertions}\n  });\n});\n";
export default {
  name: name,
  assertion: assertion,
  dispatcher: dispatcher,
  wrap: wrap
};