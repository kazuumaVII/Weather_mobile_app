import React from 'react';
import PropTypes from 'prop-types';
import { Base16Theme } from 'redux-devtools-themes';
interface Props {
    theme: Base16Theme;
    type: string;
    onClick: () => void;
    disabled?: boolean;
}
declare const SliderButton_base: typeof React.PureComponent;
export default class SliderButton extends SliderButton_base<Props> {
    static propTypes: {
        theme: PropTypes.Requireable<object>;
        type: PropTypes.Requireable<string>;
        disabled: PropTypes.Requireable<boolean>;
        onClick: PropTypes.Requireable<(...args: any[]) => any>;
    };
    iconStyle(): {
        cursor: string;
        fill: string;
        width: string;
        height: string;
    };
    renderPlayButton(): JSX.Element;
    renderPauseButton: () => JSX.Element;
    renderStepButton: (direction: 'left' | 'right') => JSX.Element;
    render(): JSX.Element | null;
}
export {};
